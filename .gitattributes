## Track large/binary assets with Git LFS
# Intentionally keep slips images, PDFs, and *.db ignored (not tracked).
# Only keep LFS rules for model artifacts that we intend to commit.

# Trained model weights and artifacts
cv_service/trained_models/**/*.pt filter=lfs diff=lfs merge=lfs -text
cv_service/trained_models/**/*.pth filter=lfs diff=lfs merge=lfs -text
cv_service/trained_models/**/*.ckpt filter=lfs diff=lfs merge=lfs -text
cv_service/trained_models/**/*.onnx filter=lfs diff=lfs merge=lfs -text

# Optional top-level trained models directory (preferred location to commit models)
trained_models/**/*.pt filter=lfs diff=lfs merge=lfs -text
trained_models/**/*.pth filter=lfs diff=lfs merge=lfs -text
trained_models/**/*.ckpt filter=lfs diff=lfs merge=lfs -text
trained_models/**/*.onnx filter=lfs diff=lfs merge=lfs -text
